<?xml version="1.0" encoding="utf-8"?>
<!-- <odoo>
   <record id="action_report_product_template" model="ir.actions.report">
       <field name="name">Product Report</field>
       <field name="model">product.template</field>
       <field name="report_type">qweb-pdf</field>
       <field name="report_name">custom_app.report_product</field>
       <field name="report_file">custom_app.report_product</field>
       <field name="print_report_name">'Product Report - %s' % (object.name)</field>
       <field name="binding_model_id" ref="product.model_product_template"/>
       <field name="binding_type">report</field>
   </record>
</odoo> -->

 <odoo>
  <record id="action_report_sale_order" model="ir.actions.report">
    <field name="name">Sale Order HReport</field>
    <field name="model">sale.order</field>
    <field name="report_type">qweb-pdf</field>
    <field name="report_name">custom_app.report_sale_order</field>
    <field name="report_file">custom_app.report_sale_order</field>
    <field name="print_report_name">'Sale Report - %s' % (object.name)</field>
    <field name="binding_model_id" ref="sale.model_sale_order"/>
    <field name="binding_type">report</field>
  </record>

 <!-- Add Payslip Batch Excel Report Action based on xlsx odoo base report module  -->
 
<!-- In default Odoo, generating an XLSX report from the "Print" button isn't supported.
 However, you can enable this functionality by installing the OCA module, report_xlsx.
  After installing this module, you can define an XLSX report action as follows: -->
  
  <record id="action_report_payslip_batches" model="ir.actions.report">
    <field name="name">Payslip Batch Excel Report</field>
    <field name="model">hr.payslip.run</field>
    <field name="report_type">xlsx</field>
    <field name="report_name">custom_app.report_payslip</field>
    <field name="report_file">custom_app.report_payslip</field>
    <field name="print_report_name">'Payslip batch- %s'</field>
    <field name="binding_model_id" ref="bi_hr_payroll.model_hr_payslip_run"/>
    <field name="binding_type">report</field>
    <field name="attachment_use" eval="False"/>
</record>


</odoo> 
